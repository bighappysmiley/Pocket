# Pocket firmware — Waveshare ESP32-S3-ePaper-3.97
# Spec: Parts A–C (B/C override A for onboarding + hardware)
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(pocket)