#include "pocket/config.hpp"
// PIN helpers live in config_store.cpp; this file reserved for mbedtls SHA-256 on device.
namespace pocket {
// device pin.cpp may override hash with mbedtls in ESP builds
}